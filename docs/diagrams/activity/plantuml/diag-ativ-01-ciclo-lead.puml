@startuml

title FokusLead - Atividade Ciclo do Lead

start
:Receber lead;
:Registrar origem e contato;
if (Dados validos?) then (Sim)
  :Qualificar lead;
  if (Lead qualificado?) then (Sim)
    :Criar oportunidade;
    :Em negociacao;
    if (Fechou?) then (Sim)
      :Converter em cliente;
      :Registrar venda;
      stop
    else (Nao)
      :Marcar como perdido;
      stop
    endif
  else (Nao)
    :Solicitar mais informacoes;
    :Agendar follow-up;
    stop
  endif
else (Nao)
  :Solicitar correcao de dados;
  stop
endif

@enduml
